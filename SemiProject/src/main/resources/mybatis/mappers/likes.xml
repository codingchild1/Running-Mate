<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="com.mulcam.run.dao.LikesDAO">

	<insert  id="insertRoute" parameterType="likes">
		insert into likes(likes_no, user_id, board_type, board_no)
			values (#{likes_no}, #{user_id}, #{board_type}, #{board_no})
	</insert>
	
	<select  id="selectLikes" parameterType="likes" resultType="boolean">
		select EXISTS (select * from likes 
			where user_id= #{user_id} and board_type=#{board_type} and board_no=#{board_no} limit 1) as success;

	</select>

</mapper>